/* eslint-env node, mocha */
// import MyClass from '.';
import {strictEqual} from 'assert';

describe('/classes/MyClass', () => {
	it('Basic tests', () => {
		// deepEqual(AAAAAAAA(123), []);
		strictEqual(true, true, '');
		// deepStrictEqual(AAAAAAAA(123), [true]);
	});
});
