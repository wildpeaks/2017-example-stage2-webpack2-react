/* eslint-env node, protractor, jasmine */

describe('MyComponent', () => {
	it('test-1.js', () => {
		browser.ignoreSynchronization = true;
		browser.driver.manage().window().setSize(800, 600);

		browser.get('https://angularjs.org/');
		expect(browser.getCurrentUrl()).toBe('https://angularjs.org/');
		// expect(browser.getCurrentUrl()).toBe('https://BAD');

		//
		// TODO finish the middleware in the Protractor config
		//

		// browser.get(`https://locahost:8001/MyComponent/no-props`); // webpack-dev-server but only for the tests
		// browser.get(`https://locahost:8001/?suite=MyComponent/test-1`); // webpack-dev-server but only for the tests
		// browser.wait(() => element(by.tagName('canvas')).isPresent(), 5000);
		//
		// browser.sleep(2500);
		// browser.takeScreenshot().then(data => {
		// 	const stream = fs.createWriteStream(path.join(__dirname, `screenshots/${id}.png`));
		// 	stream.write(new Buffer(data, 'base64'));
		// 	stream.end();
		// });
	});

	// Etc.. more tests
	// it('test-2.js');
	// it('test-3.js');
	// it('test-4.js');
	// it('test-5.js');
});
